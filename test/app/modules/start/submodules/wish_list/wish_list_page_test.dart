import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('WishListPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WishListPage(title: 'WishList')));
    //  final titleFinder = find.text('WishList');
    //  expect(titleFinder, findsOneWidget);
  });
}
