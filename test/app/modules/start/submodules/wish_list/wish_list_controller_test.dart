import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:comic_shop/app/modules/start/submodules/wish_list/wish_list_module.dart';

void main() {
  initModule(WishListModule());
  // WishListController wishlist;
  //
  setUp(() {
    //     wishlist = WishListModule.to.get<WishListController>();
  });

  group('WishListController Test', () {
    //   test("First Test", () {
    //     expect(wishlist, isInstanceOf<WishListController>());
    //   });

    //   test("Set Value", () {
    //     expect(wishlist.value, equals(0));
    //     wishlist.increment();
    //     expect(wishlist.value, equals(1));
    //   });
  });
}
