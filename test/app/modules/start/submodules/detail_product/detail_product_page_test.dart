import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DetailProductPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DetailProductPage(title: 'DetailProduct')));
    //  final titleFinder = find.text('DetailProduct');
    //  expect(titleFinder, findsOneWidget);
  });
}
